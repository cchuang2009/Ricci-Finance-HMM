# Modules

- data.py — download_market_data(), prices_to_returns()
- graph.py — build_graph_from_window(), compute_components()
- capital.py — attach_capital_attributes()
- ricci.py — compute_ricci_curvature(), run_ricci_flow()
- rolling.py — build_rolling_frames(), rolling_feature_table()
- regimes.py — compute_hmm_regimes()
- story.py — build_frame_stories()
- visualization.py — visualize_network_3d(), build_3d_ricci_capital_animation()
